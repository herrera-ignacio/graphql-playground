# README

## Run Project

```
yarn install
yarn dev
```

Server will be running on _http://localhost:4000_.

## Test Queries

You can try the following queries from your browser:

```
query {
    welcome
    hello(name: "Nacho")
}
```
